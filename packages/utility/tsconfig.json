{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    // src/client runs in the browser, so it needs the DOM globals the base
    // config leaves out (window, setTimeout, and friends).
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src", "generated"]
}
