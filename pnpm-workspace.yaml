packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks postinstall scripts by default; Prisma needs its
# scripts to download the query engine binaries.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - prisma
  - sharp
